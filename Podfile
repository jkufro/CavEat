# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'CavEat' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for CavEat
  pod 'Alamofire', '~> 4.8'
  pod 'Japx', '~> 2.1'
  pod 'Japx/CodableAlamofire'
  pod 'SwiftLint'

  target 'CavEatTests' do
    inherit! :search_paths
    # Pods for testing

  end

end
